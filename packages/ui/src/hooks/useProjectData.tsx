import { WcxNode } from "types";

export default function useProjectData(id: string) {
  //DB에서 모달 id를 추출해 알맞는 모달 데이터 불러온다.
  let nodeData: WcxNode | null;
  //Id에 해당하는 모달 노드 데이터를 반환한다.
  return nodeData;
}
